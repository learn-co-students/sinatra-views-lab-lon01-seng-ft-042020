<!DOCTYPE html>
<html>
  <body>
    <h1>Today</h1>
    <% week = Time.now.strftime("%A") %>
    <% month = Time.now.strftime("%B") %>
    <% day = Time.now.strftime("%d") %>    
    <% year = Time.now.strftime("%Y") %>
    <p><%= "The date is #{week}, #{month} #{day}, #{year}" %></p>
  </body>
</html>